@model BlogModel

<div class="container" mt-5>

    @*<form asp-controller="User" asp-action="Create" method="post">
        <div class="col-12">
            <div class="row">
                <div class="col-3">Title</div>
                <div class="col-9">
                    <input type="text" name="Title" asp-for="@Model.post.Title" placeholder="The Post Title" />
                </div>
            </div>

            <div class="row">
                <div class="col-3">Content</div>
                <div class="col-9">
                    <input type="text" name="Content" asp-for="@Model.post.Content" placeholder="The Post Content" />
                </div>
            </div>
        </div>
    </form>*@

    <form asp-controller="User" asp-action="Create" method="post">
        <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label">Title</label>
            <input type="text" class="form-control" id="postTitle" name="Title" asp-for="@Model.post.Title" placeholder="The Post Title" aria-describedby="postTitleHelp" required />
            <div id="postTitleHelp" class="form-text">Insert the text to be shown as the Post Title.</div>
        </div>
        <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Content</label>
            <textarea class="form-control" id="postContent" name="Content" asp-for="@Model.post.Content" placeholder="The Post Content" describedby="postTitleHelp" required ></textarea>
            <div id="postTitleHelp" class="form-text">Insert the text to be shown as the Post Content.</div>
        </div>
        <input class="btn btn-success" type="submit" value="Create" />
    </form>

</div>